import client from '../../../util/api-client';

// {{identifiers.label_plural}}
export const get{{identifiers.pascal_plural}} = params => client.fetch('{{identifiers.kebab_plural}}', params);
export const get{{identifiers.pascal_singular}} = id => client.fetch(`{{identifiers.kebab_plural}}/${id}`);
export const create{{identifiers.pascal_singular}} = params => client.create('{{identifiers.kebab_plural}}', params);
export const update{{identifiers.pascal_singular}} = (id, params) => client.update(`{{identifiers.kebab_plural}}/${id}`, params);
export const delete{{identifiers.pascal_singular}} = id => client.destroy(`{{identifiers.kebab_plural}}/${id}`);

{{#if-has-feature "Sortable"}}
    export const move{{identifiers.pascal_singular}} = (id, direction) => client.put(`{{identifiers.kebab_plural}}/${id}/move`, { direction });
{{/if-has-feature}}
