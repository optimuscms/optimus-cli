{
  "id": "sub-services", // kebab
  "name": "Sub Services", // Inferred by default title of id

  "fields": [
    {
      "type": "string",
      "name": "banner_heading",
      "label": "Banner Heading",

      "options": {},

      "rules": {
        "required": true, // Inferred as false
        "nullable": false, // Inferred as false
        "url": false, // Inferred as false
        "max": 255 // Inferred as null
      }
    },

    {
      "type": "string",
      "name": "banner_subheading",
      "label": "Banner Subeading",

      "options": {},

      "rules": {
        "required": true, // Inferred as false
        "nullable": false, // Inferred as false
        "url": false, // Inferred as false
        "max": 255 // Inferred as null
      }
    },

    {
      "type": "string",
      "name": "introduction_subheading",
      "label": "Introduction Subeading",

      "options": {},

      "rules": {
        "required": true, // Inferred as false
        "nullable": false, // Inferred as false
        "url": false, // Inferred as false
        "max": 255 // Inferred as null
      }
    },

    {
      "type": "string",
      "name": "introduction_subheading",
      "label": "Introduction Subeading",

      "options": {},

      "rules": {
        "required": true, // Inferred as false
        "nullable": false, // Inferred as false
        "url": false, // Inferred as false
        "max": 255 // Inferred as null
      }
    },

    {
      "type": "string",
      "name": "introduction_subheading",
      "label": "Introduction Subeading",

      "options": {},

      "rules": {
        "required": true, // Inferred as false
        "nullable": false, // Inferred as false
        "url": false, // Inferred as false
        "max": 255 // Inferred as null
      }
    },

    {
      "type": "string",
      "name": "introduction_content",
      "label": "Introduction Content",

      "options": {},

      "rules": {
        "required": true, // Inferred as false
        "nullable": false, // Inferred as false
        "url": false, // Inferred as false
        "max": 255 // Inferred as null
      }
    },

    {
      "type": "image",
      "name": "banner_image",
      "label": "Banner Image",

      "rules": {
        "required": true, // false by default
        "nullable": false, // false by default
        "url": false
      },

      "options": {
        "accepted_extensions": "image", // any by default
        "group": "image",
        "conversions": ["1400x600"]
      }
    }
  ]
}
